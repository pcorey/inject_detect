<p>We've detected an <b>unexpected query</b> in your "<%= @application.name %>" application.</p>

<p><a href="<%= InjectDetect.Endpoint.url %>/unexpected-query/<%= @unexpected_query.id %>" style="color: #ea5e5e;">Click here</a> to see the structure of the unexpected query, the number of times we've detected it, and the most similar expected query was have on file for your application.</p>

<p>For more information on unexpected queries and NoSQL Injection, take a look at <a href="#" style="color: #ea5e5e;">our FAQ and guides</a>.</p>
